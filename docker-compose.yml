services:
  frontend:
    build:
      context: .
      dockerfile: cicd/docker/frontend.Dockerfile
    ports:
      - "5173:5173"
    env_file:
      - .env.example

  backend:
    build:
      context: .
      dockerfile: cicd/docker/backend.Dockerfile
    ports:
      - "8000:8000"
    env_file:
      - .env.example
