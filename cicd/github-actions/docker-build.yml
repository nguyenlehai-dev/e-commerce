name: Docker Build

on:
  push:
    branches: [develop, staging, main]

jobs:
  docker-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker compose build
